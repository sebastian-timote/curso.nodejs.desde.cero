extends layout.pug

block content
    div.error
        h1 Error N~ #{error.status}
        h2= error.statusText
        a.button.show(href="/") home
        div.item
            pre #{error.stack}
        img(src="indentation-war.png")
